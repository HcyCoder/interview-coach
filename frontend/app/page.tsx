import { ChatShell } from '@/components/chat-shell';

/**
 * Page renders the default interview coach landing screen.
 *
 * Inputs: none.
 * Outputs: the main interview coaching workspace.
 */
export default function Page() {
  return <ChatShell />;
}
